import { SearchRoomPipe } from './search-room.pipe';

describe('SearchRoomPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchRoomPipe();
    expect(pipe).toBeTruthy();
  });
});
