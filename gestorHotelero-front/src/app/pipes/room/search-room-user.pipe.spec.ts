import { SearchRoomUserPipe } from './search-room-user.pipe';

describe('SearchRoomUserPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchRoomUserPipe();
    expect(pipe).toBeTruthy();
  });
});
