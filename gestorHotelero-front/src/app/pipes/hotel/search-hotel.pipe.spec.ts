import { SearchHotelPipe } from './search-hotel.pipe';

describe('SearchHotelPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchHotelPipe();
    expect(pipe).toBeTruthy();
  });
});
