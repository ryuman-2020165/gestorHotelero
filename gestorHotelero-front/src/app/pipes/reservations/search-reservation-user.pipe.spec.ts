import { SearchReservationUserPipe } from './search-reservation-user.pipe';

describe('SearchReservationUserPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchReservationUserPipe();
    expect(pipe).toBeTruthy();
  });
});
